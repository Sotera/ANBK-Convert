<UserControl x:Class="JistBridge.UI.ReportList.ReportList"
						 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
						 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
						 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
						 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
						 xmlns:valueConverters="clr-namespace:JistBridge.UI.ValueConverters"
						 mc:Ignorable="d"
						 d:DesignHeight="300"
						 d:DesignWidth="150">
	<UserControl.Resources>
		<valueConverters:ReportToTextConverter x:Key="ReportToTextConverter" />
	</UserControl.Resources>
	<ListBox ItemsSource="{Binding Reports}"
					 SelectionMode="Single"
					 SelectedItem="{Binding SelectedReport}"
					 IsSynchronizedWithCurrentItem="True">
		<ListBox.ItemTemplate>
			<DataTemplate>
				<StackPanel Orientation="Horizontal">
					<CheckBox IsChecked="{Binding ReportVisible, Mode=TwoWay}"/>
					<Label Content="{Binding ShortName}"/>
				</StackPanel>
			</DataTemplate>
		</ListBox.ItemTemplate>
	</ListBox>
</UserControl>
